/**
 * Tailwind CSS config (legacy JS file)
 * Nota: El proyecto ya usa Tailwind v4 con @theme en CSS.
 * Este archivo existe solo como referencia para otros asistentes
 * y para permitir autocompletado de clases como text-brandRed/bg-brandRed.
 */

export default {
  theme: {
    extend: {
      colors: {
        brandRed: '#FF3131',
      },
    },
  },
  plugins: [],
};
